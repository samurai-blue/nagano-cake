<div class = "container">
  <div class ="row">
    <h1>商品詳細</h1>   
  </div>
  <div class ="row">
    <div class = "col-auto">
      <%= attachment_image_tag @item, :image %>
    </div>
    <div class = "col-auto">
      <h2><%= @item.name %></h2>
      <h4><%= @item.description %></h4>
      <h4>¥<%= @item.price %>円（税込み）</h4>
    </div>
  </div>
</div>

